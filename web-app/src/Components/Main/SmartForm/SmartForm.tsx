import React from 'react'
import { FormData } from '../../../../../packages/src/interfaces'

export interface SmartFormProps {
    formData: FormData
}

const SmartForm = ({ formData } : SmartFormProps) => (
    <div></div>
)

export default SmartForm;