import { expect } from 'chai';
import {  convertFormDescriptorsToFormDataArray, mergeFormDescriptorArrayWithFormDataArray } from '../../src/helper/formsConverter';

describe('convertFormDescriptorsToFormDataArray function', () => {
    it('should convert form descriptors to a form data array', () => {
        
    })
})

describe('mergeFormDescriptorArrayWithFormDataArray function', () => {
    it('should merge a form descriptors to a form data array', () => {
        
    })
})